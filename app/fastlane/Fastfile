default_platform(:android)

platform :android do
  desc "Build the Android app (Generate .aab)"

  lane :deploy do
    gradle(
      task: "bundleRelease", 
      project_dir: "../",     
    )
  end
end