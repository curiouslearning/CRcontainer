<?xml version="1.0" encoding="utf-8"?>
<!-- Pill selected: purple fill + border, rounded (matches list theme). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#8A4DFF" />
    <corners android:radius="26dp" />
    <stroke android:width="1dp" android:color="@color/dropdown_list_border" />
</shape>