<?xml version="1.0" encoding="utf-8"?>
<!-- Pill item: centered text, spacing between items, min 48dp touch target; no logic/ID changes. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center"
    android:paddingStart="22dp"
    android:paddingEnd="22dp"
    android:paddingTop="14dp"
    android:paddingBottom="14dp"
    android:minHeight="64dp"
    android:layout_marginBottom="@dimen/dropdown_item_spacing"
    android:background="@drawable/dropdown_item_pill_default">

    <TextView
        android:id="@android:id/text1"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="#FF2D1B47"
        android:textSize="17sp"
        android:fontFamily="sans-serif-medium"
        android:maxLines="3"
        android:ellipsize="none"
        android:gravity="center"
        android:singleLine="false" />

    <ImageView
        android:id="@+id/checkmark"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_marginStart="12dp"
        android:src="@drawable/checkmark_white"
        android:tint="@android:color/white"
        android:visibility="gone"
        android:contentDescription="@string/selected" />
</LinearLayout>